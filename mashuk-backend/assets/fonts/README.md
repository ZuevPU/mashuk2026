# PDF fonts (DejaVu)

`DejaVuSans.ttf` and `DejaVuSans-Bold.ttf` — free fonts with Cyrillic coverage for PDFKit profile export.

License: Bitstream Vera / DejaVu (see https://dejavu-fonts.github.io/).

Fallback: npm package `dejavu-fonts-ttf` if files are missing at runtime.
