/**
 * Рассылки контента — только руками из админки
 * (Уведомления → По дням / Отправить сейчас / Запланировать).
 *
 * Выключено: слоты 08:00–23:00, догонялки, пуш при публикации вопроса/задания,
 * планировщик точек, напоминания о слотах программы.
 *
 * Личные сервисные пуши (заявка принята/отклонена, медаль, команда) не трогаем.
 */
export const AUTO_CONTENT_PUSH_ENABLED = false;

export function allowAutoContentPush(): boolean {
  return AUTO_CONTENT_PUSH_ENABLED === true;
}
